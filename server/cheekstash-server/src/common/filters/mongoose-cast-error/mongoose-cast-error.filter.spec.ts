import { MongooseCastErrorFilter } from './mongoose-cast-error.filter';

describe('MongooseCastErrorFilter', () => {
  it('should be defined', () => {
    expect(new MongooseCastErrorFilter()).toBeDefined();
  });
});
